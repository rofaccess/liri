RSpec.describe Liri do
  context 'run' do
    it "does something useful" do
      expect(false).to eq(true)
    end

    it "has a version number" do
      expect(Liri::VERSION).not_to be nil
    end
  end
end